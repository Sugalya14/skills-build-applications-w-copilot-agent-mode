# OctoFit Tracker — Frontend

This folder is reserved for the React frontend for OctoFit Tracker. Start the frontend using Create React App or Vite; typical structure will live under `octofit-tracker/frontend`.

Suggested quick start:

```bash
cd octofit-tracker/frontend
npm init vite@latest . --template react
npm install
npm run dev
```
